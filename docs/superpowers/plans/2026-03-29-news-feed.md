# News Feed Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expose a `/news` page showing signal-linked articles sorted by signal opportunity score, with client-side filtering by direction and event type.

**Architecture:** A new `GET /news/feed` backend endpoint joins signals + news_articles in Python (no DB join), deduplicates articles by highest-scoring signal, and returns flat `NewsFeedItem` objects. A Next.js server page fetches and passes the list to a `'use client'` `NewsFeed` component that owns filter state. `NewsCard` is a pure presentational component.

**Tech Stack:** Python/FastAPI (backend), Supabase (database), Next.js Server + Client Components, TypeScript, React Testing Library.

---

## File Map

**Create:**
- `backend/tests/test_news_feed.py`
- `frontend/app/api/news/feed/route.ts`
- `frontend/components/news/NewsCard.tsx`
- `frontend/components/news/__tests__/NewsCard.test.tsx`
- `frontend/components/news/NewsFeed.tsx`
- `frontend/components/news/__tests__/NewsFeed.test.tsx`
- `frontend/app/news/page.tsx`

**Modify:**
- `backend/app/models/news.py` — add `NewsFeedItem`
- `backend/app/routers/news.py` — add `GET /feed` (before `/{article_id}`)
- `frontend/lib/types.ts` — add `NewsFeedItem` interface
- `frontend/lib/api.ts` — add `getNewsFeed()`

---

## Task 1: Backend model + GET /news/feed endpoint

**Files:**
- Modify: `backend/app/models/news.py`
- Modify: `backend/app/routers/news.py`
- Create: `backend/tests/test_news_feed.py`

**Context:** The existing `news.py` router has `GET ""` (list) and `GET /{article_id}`. The new `/feed` route must go **before** `/{article_id}` to avoid FastAPI treating "feed" as an article_id. The endpoint fetches all signals ordered by `opportunity_score DESC`, extracts `evidence["article_ids"]` from each, deduplicates (first signal wins = highest score), batch-fetches those articles, attaches signal context, and returns sorted. Uses `Optional` from `typing` — no `float | None` syntax (Python 3.9).

Currently 99 backend tests pass.

- [ ] **Step 1: Add `NewsFeedItem` model to news.py**

Read `backend/app/models/news.py` first. Append at the bottom:

```python
class NewsFeedItem(BaseModel):
    id: str
    headline: str
    url: str
    published_at: Optional[datetime] = None
    sentiment_score: Optional[float] = None
    event_type: Optional[str] = None
    credibility_score: Optional[float] = None
    tickers: list[str] = []
    signal_direction: str
    signal_confidence: float
    signal_opportunity_score: float
```

- [ ] **Step 2: Write failing tests**

Create `backend/tests/test_news_feed.py`:

```python
import pytest
from unittest.mock import MagicMock
from datetime import datetime, timezone


def _signal(id="sig-1", direction="bullish", confidence=0.75, opp_score=0.8, article_ids=None):
    return {
        "id": id,
        "direction": direction,
        "confidence": confidence,
        "opportunity_score": opp_score,
        "evidence": {"article_ids": article_ids or []},
    }


def _article(id="art-1", headline="Market moves", event_type="earnings", tickers=None):
    return {
        "id": id,
        "headline": headline,
        "url": "https://example.com/article",
        "published_at": datetime.now(timezone.utc).isoformat(),
        "sentiment_score": 0.42,
        "event_type": event_type,
        "credibility_score": 0.8,
        "tickers": tickers or ["AAPL"],
    }


def _setup_client(mock_db, signals_data, articles_data):
    """Set up mock_db to return signals and articles from separate tables."""
    signals_tbl = MagicMock()
    signals_tbl.select.return_value.order.return_value.execute.return_value = MagicMock(
        data=signals_data
    )
    articles_tbl = MagicMock()
    articles_tbl.select.return_value.in_.return_value.execute.return_value = MagicMock(
        data=articles_data
    )

    def side(t):
        if t == "signals":
            return signals_tbl
        if t == "news_articles":
            return articles_tbl
        return MagicMock()

    mock_db.table.side_effect = side


def test_news_feed_returns_empty_when_no_signals(client):
    c, mock_db = client
    signals_tbl = MagicMock()
    signals_tbl.select.return_value.order.return_value.execute.return_value = MagicMock(data=[])
    mock_db.table.return_value = signals_tbl
    response = c.get("/news/feed")
    assert response.status_code == 200
    assert response.json() == []


def test_news_feed_returns_articles_ordered_by_opp_score(client):
    c, mock_db = client
    signals = [
        _signal(id="sig-1", direction="bullish", opp_score=0.9, article_ids=["art-1"]),
        _signal(id="sig-2", direction="bearish", opp_score=0.5, article_ids=["art-2"]),
    ]
    articles = [
        _article(id="art-2", headline="Low story"),
        _article(id="art-1", headline="Top story"),
    ]
    _setup_client(mock_db, signals, articles)
    response = c.get("/news/feed")
    assert response.status_code == 200
    data = response.json()
    assert len(data) == 2
    assert data[0]["headline"] == "Top story"
    assert data[0]["signal_opportunity_score"] == 0.9


def test_news_feed_deduplicates_articles(client):
    c, mock_db = client
    # Both signals reference the same article — bullish (0.9) wins
    signals = [
        _signal(id="sig-1", direction="bullish", opp_score=0.9, article_ids=["art-1"]),
        _signal(id="sig-2", direction="bearish", opp_score=0.5, article_ids=["art-1"]),
    ]
    articles = [_article(id="art-1")]
    _setup_client(mock_db, signals, articles)
    response = c.get("/news/feed")
    data = response.json()
    assert len(data) == 1
    assert data[0]["signal_direction"] == "bullish"


def test_news_feed_filters_by_direction(client):
    c, mock_db = client
    signals = [
        _signal(id="sig-1", direction="bullish", opp_score=0.9, article_ids=["art-1"]),
        _signal(id="sig-2", direction="bearish", opp_score=0.7, article_ids=["art-2"]),
    ]
    articles = [_article(id="art-1"), _article(id="art-2")]
    _setup_client(mock_db, signals, articles)
    response = c.get("/news/feed?direction=bullish")
    data = response.json()
    assert len(data) == 1
    assert data[0]["signal_direction"] == "bullish"


def test_news_feed_filters_by_event_type(client):
    c, mock_db = client
    signals = [
        _signal(id="sig-1", opp_score=0.9, article_ids=["art-1"]),
        _signal(id="sig-2", opp_score=0.7, article_ids=["art-2"]),
    ]
    articles = [
        _article(id="art-1", event_type="earnings"),
        _article(id="art-2", event_type="regulation"),
    ]
    _setup_client(mock_db, signals, articles)
    response = c.get("/news/feed?event_type=earnings")
    data = response.json()
    assert len(data) == 1
    assert data[0]["event_type"] == "earnings"


def test_news_feed_skips_signals_without_article_ids(client):
    c, mock_db = client
    signals_tbl = MagicMock()
    signals_tbl.select.return_value.order.return_value.execute.return_value = MagicMock(data=[
        {"id": "sig-1", "direction": "bullish", "confidence": 0.75,
         "opportunity_score": 0.9, "evidence": None},
        {"id": "sig-2", "direction": "bullish", "confidence": 0.75,
         "opportunity_score": 0.8, "evidence": {"article_ids": []}},
    ])
    mock_db.table.return_value = signals_tbl
    response = c.get("/news/feed")
    assert response.status_code == 200
    assert response.json() == []
```

- [ ] **Step 3: Run tests to confirm they fail**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/backend" && .venv/bin/pytest tests/test_news_feed.py -v
```
Expected: `404 Not Found` or import errors (route doesn't exist yet).

- [ ] **Step 4: Add `GET /feed` endpoint to news.py**

Read `backend/app/routers/news.py` first. Update the import line to add `NewsFeedItem`:

```python
from app.models.news import NewsArticleResponse, PaginatedNews, NewsFeedItem
```

Then insert the new endpoint **before** the existing `@router.get("/{article_id}", ...)` route:

```python
@router.get("/feed", response_model=list[NewsFeedItem])
def get_news_feed(
    direction: Optional[str] = Query(None),
    event_type: Optional[str] = Query(None),
    db: Client = Depends(get_db),
):
    signals = (
        db.table("signals")
        .select("id, direction, confidence, opportunity_score, evidence")
        .order("opportunity_score", desc=True)
        .execute()
        .data or []
    )

    # Build article_id → signal map; first match wins (signals are DESC by opp_score)
    article_signal: dict = {}
    for sig in signals:
        evidence = sig.get("evidence") or {}
        for aid in (evidence.get("article_ids") or []):
            if aid not in article_signal:
                article_signal[aid] = sig

    if not article_signal:
        return []

    articles = (
        db.table("news_articles")
        .select(
            "id, headline, url, published_at, sentiment_score, "
            "event_type, credibility_score, tickers"
        )
        .in_("id", list(article_signal.keys()))
        .execute()
        .data or []
    )

    result = []
    for article in articles:
        sig = article_signal.get(article["id"])
        if not sig:
            continue
        if direction and sig["direction"] != direction:
            continue
        if event_type and article.get("event_type") != event_type:
            continue
        result.append({
            **article,
            "signal_direction": sig["direction"],
            "signal_confidence": sig["confidence"],
            "signal_opportunity_score": sig["opportunity_score"],
        })

    result.sort(key=lambda x: x["signal_opportunity_score"], reverse=True)
    return result
```

- [ ] **Step 5: Run news feed tests**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/backend" && .venv/bin/pytest tests/test_news_feed.py -v
```
Expected: `6 passed`

- [ ] **Step 6: Run full backend suite**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/backend" && .venv/bin/pytest --tb=short -q
```
Expected: `105 passed` (99 existing + 6 new)

- [ ] **Step 7: Commit**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI" && git add backend/app/models/news.py backend/app/routers/news.py backend/tests/test_news_feed.py && git commit -m "feat: add NewsFeedItem model and GET /news/feed endpoint"
```

---

## Task 2: Frontend types, API helper, proxy route

**Files:**
- Modify: `frontend/lib/types.ts`
- Modify: `frontend/lib/api.ts`
- Create: `frontend/app/api/news/feed/route.ts`

**Context:** Read `frontend/lib/types.ts` and `frontend/lib/api.ts` first to understand existing structure. The `BACKEND` constant is already defined in `api.ts` — use it (do not use `process.env.BACKEND_URL` directly in api.ts). The proxy route uses `process.env.BACKEND_URL` directly, consistent with other proxy routes in the codebase.

Currently 66 frontend tests pass.

- [ ] **Step 1: Add `NewsFeedItem` type to lib/types.ts**

Read `frontend/lib/types.ts` first. Append at the end:

```typescript
export interface NewsFeedItem {
  id: string
  headline: string
  url: string
  published_at: string
  sentiment_score: number | null
  event_type: string | null
  credibility_score: number | null
  tickers: string[]
  signal_direction: string
  signal_confidence: number
  signal_opportunity_score: number
}
```

- [ ] **Step 2: Add `getNewsFeed` to lib/api.ts**

Read `frontend/lib/api.ts` first. Add `NewsFeedItem` to the existing types import, then append the new function:

```typescript
import type { ..., NewsFeedItem } from '@/lib/types'
```

```typescript
export async function getNewsFeed(): Promise<NewsFeedItem[]> {
  try {
    const res = await fetch(`${BACKEND}/news/feed`, { cache: 'no-store' })
    if (!res.ok) return []
    return res.json()
  } catch {
    return []
  }
}
```

- [ ] **Step 3: Create proxy route**

Create `frontend/app/api/news/feed/route.ts` (create parent directories as needed):

```typescript
export async function GET() {
  try {
    const res = await fetch(`${process.env.BACKEND_URL}/news/feed`, {
      cache: 'no-store',
    })
    const data = await res.json()
    return Response.json(data, { status: res.status })
  } catch {
    return Response.json({ error: 'upstream_unavailable' }, { status: 502 })
  }
}
```

- [ ] **Step 4: Confirm frontend tests still pass**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -5
```
Expected: `66 passed`

- [ ] **Step 5: Commit**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI" && git add frontend/lib/types.ts frontend/lib/api.ts frontend/app/api/news/feed/route.ts && git commit -m "feat: add NewsFeedItem type, getNewsFeed helper, and proxy route"
```

---

## Task 3: NewsCard component

**Files:**
- Create: `frontend/components/news/NewsCard.tsx`
- Create: `frontend/components/news/__tests__/NewsCard.test.tsx`

**Context:** `NewsCard` is a pure presentational component — no state, no fetch. Renders one `NewsFeedItem`. Sentiment display: if `sentiment_score >= 0`, render `+0.42` in `text-profit`; if `< 0`, render `-0.18` in `text-loss`. Source domain extracted from `url` via `new URL(url).hostname`. Direction label: `↑ Bullish` / `↓ Bearish` / `⚠ Crash Risk`. Ticker badges are rendered as `<span>` elements.

Currently 66 frontend tests pass.

- [ ] **Step 1: Write failing tests**

Create `frontend/components/news/__tests__/NewsCard.test.tsx`:

```typescript
import { render, screen } from '@testing-library/react'
import NewsCard from '../NewsCard'
import type { NewsFeedItem } from '@/lib/types'

const mockItem: NewsFeedItem = {
  id: 'art-1',
  headline: 'Apple reports record earnings',
  url: 'https://example.com/apple-earnings',
  published_at: '2026-03-20T10:00:00Z',
  sentiment_score: 0.42,
  event_type: 'earnings',
  credibility_score: 0.8,
  tickers: ['AAPL'],
  signal_direction: 'bullish',
  signal_confidence: 0.75,
  signal_opportunity_score: 0.85,
}

describe('NewsCard', () => {
  it('renders headline as a link to the article url', () => {
    render(<NewsCard item={mockItem} />)
    const link = screen.getByRole('link', { name: /apple reports record earnings/i })
    expect(link).toHaveAttribute('href', 'https://example.com/apple-earnings')
  })

  it('renders ticker badge', () => {
    render(<NewsCard item={mockItem} />)
    expect(screen.getByText('AAPL')).toBeInTheDocument()
  })

  it('renders signal direction label and confidence', () => {
    render(<NewsCard item={mockItem} />)
    expect(screen.getByText(/bullish/i)).toBeInTheDocument()
    expect(screen.getByText(/75%/i)).toBeInTheDocument()
  })

  it('renders positive sentiment with text-profit class', () => {
    render(<NewsCard item={mockItem} />)
    const el = screen.getByText('+0.42')
    expect(el).toHaveClass('text-profit')
  })

  it('renders negative sentiment with text-loss class', () => {
    render(<NewsCard item={{ ...mockItem, sentiment_score: -0.18 }} />)
    const el = screen.getByText('-0.18')
    expect(el).toHaveClass('text-loss')
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --testPathPattern="news/__tests__/NewsCard" --watchAll=false 2>&1 | tail -5
```
Expected: `Cannot find module '../NewsCard'`

- [ ] **Step 3: Implement NewsCard**

Create `frontend/components/news/NewsCard.tsx`:

```typescript
import type { NewsFeedItem } from '@/lib/types'

function directionLabel(d: string) {
  if (d === 'bullish') return '↑ Bullish'
  if (d === 'bearish') return '↓ Bearish'
  return '⚠ Crash Risk'
}

function sourceDomain(url: string) {
  try {
    return new URL(url).hostname.replace('www.', '')
  } catch {
    return url
  }
}

export default function NewsCard({ item }: { item: NewsFeedItem }) {
  const sentimentSign = item.sentiment_score !== null && item.sentiment_score >= 0 ? '+' : ''
  const sentimentClass =
    item.sentiment_score === null
      ? 'text-gray-500'
      : item.sentiment_score >= 0
      ? 'text-profit'
      : 'text-loss'

  return (
    <div className="bg-surface-card rounded-xl border border-white/8 p-4 space-y-3">
      {/* Header: tickers + direction */}
      <div className="flex items-center gap-2 flex-wrap">
        {item.tickers.map((t) => (
          <span
            key={t}
            className="text-xs font-mono font-semibold px-2 py-0.5 rounded bg-white/8 text-white"
          >
            {t}
          </span>
        ))}
        <span
          className={`text-xs font-medium ${item.signal_direction === 'bullish' ? 'text-profit' : 'text-loss'}`}
        >
          {directionLabel(item.signal_direction)} · {Math.round(item.signal_confidence * 100)}%
        </span>
      </div>

      {/* Headline */}
      <a
        href={item.url}
        target="_blank"
        rel="noopener noreferrer"
        className="block text-sm font-medium text-white leading-snug hover:text-brand-cyan transition-colors"
      >
        {item.headline}
      </a>

      {/* Footer: event type, sentiment, date, source */}
      <div className="flex items-center gap-3 flex-wrap text-xs text-gray-500">
        {item.event_type && (
          <span className="px-2 py-0.5 rounded bg-white/5 text-gray-400 capitalize">
            {item.event_type.replace('_', ' ')}
          </span>
        )}
        {item.sentiment_score !== null && (
          <span className={sentimentClass}>
            {sentimentSign}{item.sentiment_score.toFixed(2)}
          </span>
        )}
        <span>{new Date(item.published_at ?? '').toLocaleDateString()}</span>
        <span>{sourceDomain(item.url)}</span>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Run NewsCard tests**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --testPathPattern="news/__tests__/NewsCard" --watchAll=false 2>&1 | tail -5
```
Expected: `5 passed`

- [ ] **Step 5: Run full frontend suite**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -5
```
Expected: `71 passed`

- [ ] **Step 6: Commit**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI" && git add frontend/components/news/NewsCard.tsx "frontend/components/news/__tests__/NewsCard.test.tsx" && git commit -m "feat: add NewsCard component"
```

---

## Task 4: NewsFeed component

**Files:**
- Create: `frontend/components/news/NewsFeed.tsx`
- Create: `frontend/components/news/__tests__/NewsFeed.test.tsx`

**Context:** `NewsFeed` is `'use client'`. It owns two state values: `direction` (string, default `""`) and `eventType` (string, default `""`). Filtering is done by comparing `item.signal_direction === direction` (skip if direction is `""`) and `item.event_type === eventType` (skip if eventType is `""`). The two `<select>` elements use `aria-label` attributes so tests can find them with `screen.getByLabelText`. Empty state shows when filtered list is empty. Note: `@testing-library/user-event` is NOT installed — use `fireEvent.change` from `@testing-library/react` for select interaction.

Currently 71 frontend tests pass.

- [ ] **Step 1: Write failing tests**

Create `frontend/components/news/__tests__/NewsFeed.test.tsx`:

```typescript
import { render, screen, fireEvent } from '@testing-library/react'
import NewsFeed from '../NewsFeed'
import type { NewsFeedItem } from '@/lib/types'

const makeItem = (overrides: Partial<NewsFeedItem> = {}): NewsFeedItem => ({
  id: 'art-1',
  headline: 'Test headline',
  url: 'https://example.com',
  published_at: '2026-03-20T10:00:00Z',
  sentiment_score: 0.3,
  event_type: 'earnings',
  credibility_score: 0.8,
  tickers: ['AAPL'],
  signal_direction: 'bullish',
  signal_confidence: 0.75,
  signal_opportunity_score: 0.8,
  ...overrides,
})

describe('NewsFeed', () => {
  it('renders empty state when items is empty', () => {
    render(<NewsFeed items={[]} />)
    expect(screen.getByText('No signal-linked news yet')).toBeInTheDocument()
  })

  it('renders all items when no filter is active', () => {
    const items = [
      makeItem({ id: 'art-1', headline: 'Story 1' }),
      makeItem({ id: 'art-2', headline: 'Story 2' }),
    ]
    render(<NewsFeed items={items} />)
    expect(screen.getByText('Story 1')).toBeInTheDocument()
    expect(screen.getByText('Story 2')).toBeInTheDocument()
  })

  it('filters to bullish direction only', () => {
    const items = [
      makeItem({ id: 'art-1', headline: 'Bullish story', signal_direction: 'bullish' }),
      makeItem({ id: 'art-2', headline: 'Bearish story', signal_direction: 'bearish' }),
    ]
    render(<NewsFeed items={items} />)
    fireEvent.change(screen.getByLabelText('Direction'), { target: { value: 'bullish' } })
    expect(screen.getByText('Bullish story')).toBeInTheDocument()
    expect(screen.queryByText('Bearish story')).not.toBeInTheDocument()
  })

  it('filters to earnings event type only', () => {
    const items = [
      makeItem({ id: 'art-1', headline: 'Earnings story', event_type: 'earnings' }),
      makeItem({ id: 'art-2', headline: 'Regulation story', event_type: 'regulation' }),
    ]
    render(<NewsFeed items={items} />)
    fireEvent.change(screen.getByLabelText('Event Type'), { target: { value: 'earnings' } })
    expect(screen.getByText('Earnings story')).toBeInTheDocument()
    expect(screen.queryByText('Regulation story')).not.toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --testPathPattern="news/__tests__/NewsFeed" --watchAll=false 2>&1 | tail -5
```
Expected: `Cannot find module '../NewsFeed'`

- [ ] **Step 3: Implement NewsFeed**

Create `frontend/components/news/NewsFeed.tsx`:

```typescript
'use client'
import { useState } from 'react'
import type { NewsFeedItem } from '@/lib/types'
import NewsCard from './NewsCard'

export default function NewsFeed({ items }: { items: NewsFeedItem[] }) {
  const [direction, setDirection] = useState('')
  const [eventType, setEventType] = useState('')

  const filtered = items.filter((item) => {
    if (direction && item.signal_direction !== direction) return false
    if (eventType && item.event_type !== eventType) return false
    return true
  })

  return (
    <div className="space-y-4">
      {/* Filter bar */}
      <div className="flex gap-3 flex-wrap">
        <select
          aria-label="Direction"
          value={direction}
          onChange={(e) => setDirection(e.target.value)}
          className="bg-surface-card border border-white/8 rounded-lg px-3 py-2 text-sm text-gray-300 focus:outline-none focus:border-white/20"
        >
          <option value="">All Directions</option>
          <option value="bullish">Bullish</option>
          <option value="bearish">Bearish</option>
          <option value="crash_risk">Crash Risk</option>
        </select>

        <select
          aria-label="Event Type"
          value={eventType}
          onChange={(e) => setEventType(e.target.value)}
          className="bg-surface-card border border-white/8 rounded-lg px-3 py-2 text-sm text-gray-300 focus:outline-none focus:border-white/20"
        >
          <option value="">All Event Types</option>
          <option value="earnings">Earnings</option>
          <option value="regulation">Regulation</option>
          <option value="m_a">M&amp;A</option>
          <option value="product_launch">Product Launch</option>
          <option value="executive_change">Executive Change</option>
          <option value="other">Other</option>
        </select>
      </div>

      {/* Feed */}
      {filtered.length === 0 ? (
        <div className="py-16 text-center text-sm text-gray-600">
          No signal-linked news yet
        </div>
      ) : (
        <div className="space-y-3">
          {filtered.map((item) => (
            <NewsCard key={item.id} item={item} />
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 4: Run NewsFeed tests**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --testPathPattern="news/__tests__/NewsFeed" --watchAll=false 2>&1 | tail -5
```
Expected: `4 passed`

- [ ] **Step 5: Run full frontend suite**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -5
```
Expected: `75 passed`

- [ ] **Step 6: Commit**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI" && git add frontend/components/news/NewsFeed.tsx "frontend/components/news/__tests__/NewsFeed.test.tsx" && git commit -m "feat: add NewsFeed component with direction and event type filters"
```

---

## Task 5: News page

**Files:**
- Create: `frontend/app/news/page.tsx`

**Context:** This is an async Server Component — no `'use client'`. It calls `getNewsFeed()` from `@/lib/api` and passes the result to `<NewsFeed>`. `TopBar` accepts `title: string` and optional `subtitle?: string`. `Sidebar.tsx` already has the `/news` nav link — no changes needed.

Currently 75 frontend tests pass.

- [ ] **Step 1: Create the news page**

Create `frontend/app/news/page.tsx`:

```typescript
import { getNewsFeed } from '@/lib/api'
import NewsFeed from '@/components/news/NewsFeed'
import TopBar from '@/components/layout/TopBar'

export default async function NewsPage() {
  const items = await getNewsFeed()
  return (
    <div>
      <TopBar title="News" subtitle="Signal-linked market news" />
      <div className="p-6">
        <NewsFeed items={items} />
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Run full frontend suite**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/frontend" && npm test -- --passWithNoTests --watchAll=false 2>&1 | tail -5
```
Expected: `75 passed` (no new tests for the page itself — it's a thin server component)

- [ ] **Step 3: Run full backend suite**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI/backend" && .venv/bin/pytest --tb=short -q 2>&1 | tail -3
```
Expected: `105 passed`

- [ ] **Step 4: Commit**

```bash
cd "/Users/chidoziejim/Documents/CLAUDE PROJECTS/Market Pulse AI" && git add frontend/app/news/page.tsx && git commit -m "feat: add /news page"
```
